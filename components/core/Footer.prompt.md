Every page ends the same way: the email address rendered huge, as the real CTA. Small social links + a meta line beneath.

```jsx
<Footer email="hello@samfield.co" socials={[{ label: 'Instagram', href: '#' }]} />
```
