Sticky, glassy top bar. Wordmark left (no logomark exists — see readme Iconography), links + primary Contact button right. Active link gets a flame underline.

```jsx
<Nav active="work" onNavigate={(key) => setPage(key)} />
```
